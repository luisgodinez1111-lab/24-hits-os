# pnpm: usar node_modules aislado y evitar hoisting accidental de tipos.
# Dependencias no declaradas fallan explícitamente (mejor para monorepo grande).
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false
