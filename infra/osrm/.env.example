# Token del túnel de Cloudflare (panel Zero Trust → Networks → Tunnels → tu túnel
# → Install connector → copia el valor de `--token`). Cópialo a un archivo .env
# real (que NO se sube a git) junto a este ejemplo:
#   cp .env.example .env  &&  edita .env
TUNNEL_TOKEN=pega-aqui-el-token-del-tunel
